{% for  page in snippets|paginate(2) %}
    <div class="row col-md-12">
        {% for snippet in page %}
            <section class="col-md-6">
                {% include "snippet-row.twig" %}
            </section>
        {% endfor %}
    </div>
{% endfor %}