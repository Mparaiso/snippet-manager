{% if item_count && item_per_page %}
<ul class="pager">
    {% if offset>0 %}
    <li class="previous"><a href="?&offset={{ offset-1 }}&q={{q}}">Previous</a></li>
    {% endif %}
    {% if (item_count % item_per_page) == 0 && item_count != 0 %}
    <li class="next">
        <a href="?&offset={{ offset+1 }}&q={{q}}">Next</a>
    </li>
    {% endif %}
</ul>
{% endif %}