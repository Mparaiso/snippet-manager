<?php
#@note @silex bootstrapping une application en ligne de commande
$autoload = require(__DIR__."/../vendor/autoload.php");
$autoload->add("",__DIR__."/../app");
$app = new SnippetManager(array("debug"=>true));
$cli = new Console($app);

$cli->run();